#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int count ;
    float value, high, low, sum, average, range ;
    sum = 0 ;
    count = 0 ;
    printf("Enter numbers in a line : input a NEGATIVE number to end\n") ;
    input:
        scanf("%f", &cva)
    return 0 ;
}
